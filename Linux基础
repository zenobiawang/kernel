Linux基础

根文件系统（rootfs）的概念
1、“文件”不仅是指硬盘上的数据，它还包括任何设备资源。“文件”是内核范畴的概念，磁盘、U盘、内存、网络，甚至CPU都被内核抽象为“文件”。
2、并不是所有的目录和文件都对应磁盘上的存储空间。比如，sys、proc、dev对应的不是存储空间，而是设备文件，这三个目录中的内容由内核及相应的驱动程序维护。
3、“存储类文件系统”不能与rootfs并列存在，而只能挂载到rootfs下的一个子目录上。
4、Unix-like中“存数类文件系统”内部则等同于Windows中的文件系统，包括文件系统类型。
